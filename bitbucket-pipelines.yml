# This is a sample build configuration for Python.
# Check our guides at https://confluence.atlassian.com/x/x4UWN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: python:3.7.3

pipelines:
  default:
    - step:
        caches:
          - pip
        script:
          - pipe: atlassian/trigger-pipeline:4.1.2
            variables:
              BITBUCKET_USERNAME: 'gemmahentschmishcon'
              BITBUCKET_APP_PASSWORD: 'ktdFzZPzEQHQBKm73tz4'
              REPOSITORY: 'brat-compose'
              ACCOUNT: 'mdrdatascience' # Optional
              # BRANCH_NAME: '<string>' # Optional
              # CUSTOM_PIPELINE_NAME: '<string>' # Optional
              # PIPELINE_VARIABLES: '<json>' # Optional
              # WAIT: '<boolean>' # Optional
              # WAIT_MAX_TIMEOUT: '<string>' # Optional
              # DEBUG: '<boolean>' # Optional

